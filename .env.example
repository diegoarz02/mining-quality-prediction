# Copy to .env and fill in. Required only for the LLM path of the Level 5 reasoning graph.
# The graph runs without it (deterministic rule-based fallback).
#
# Generate a token at: GitHub -> Settings -> Developer settings ->
# Fine-grained tokens -> Permissions -> Models: Read-only.
GITHUB_TOKEN=
